@using BitOrchestraTestTask.BL
@model List<User>
<table>
    <thead>
        <tr>
            <th hidden>Id</th>
            <th>Name</th>
            <th>DateOfBirth</th>
            <th>Married</th>
            <th>Phone</th>
            <th>Salary</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count(); i++) 
        {
            <tr>
                <td hidden>@Model[i].Id</td>
                <td>@Model[i].Name</td>
                <td>@Model[i].DateOfBirth.ToString()</td>
                <td>@Model[i].Married</td>
                <td>@Model[i].Phone</td>
                <td>@Model[i].Salary.ToString()</td>
                <td>@Html.ActionLink("Edit", "Edit", new { id = Model[i].Id })</td>
                @*<a href="Delete/@Model[i].Id">Delete</a>*@
                <td>@Html.ActionLink("Delete", "Delete", new {id=Model[i].Id})</td>
            </tr>
        }
    </tbody>
</table>